# Server
PORT=4000
NODE_ENV=development
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-a-long-random-string"
JWT_EXPIRES_IN="30d"

# Client (Vite dev only; in production the client is served by the server on the same origin)
VITE_API_BASE_URL="/api"
